type Heartbeat @entity {
    id: ID!
    blockNumber: BigInt
    authorityId: String
    validator: String
    peerId: String
    networkAddress: String
    multiAddress: Boolean
    ipv4: String
}

type LastHeartbeat @entity{
    id: ID!
    heartbeatId: String!
}